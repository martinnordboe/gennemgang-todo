<Window x:Class="TodoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TodoApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="900">
    <Window.Resources>
        <DataTemplate x:Key="TodoItemTemplate">
            <Grid Margin="10" Background="LightSteelBlue">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <CheckBox Margin="0, 0, 0 ,0" x:Name="CheckBoxDone" IsChecked="{Binding IsDone}" Checked="CheckBoxDone_Checked" Unchecked="CheckBoxDone_Unchecked" />
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1"  VerticalAlignment="Center" HorizontalAlignment="Left">
                    <TextBlock Margin="10, 0, 0 ,0" x:Name="TextBlockTitle" Text="{Binding Title}" />
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <TextBlock Margin="10, 0, 0 ,0" x:Name="TextBlockDateOnly" Text="{Binding Date, StringFormat=yyyy-MM-dd}" />
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <TextBlock Margin="10, 0, 0 ,0" x:Name="TextBlockDone" Text="{Binding IsDone}" />
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <Button x:Name="EditToDoButton" Width="50" Height="30" Background="Aquamarine" Click="EditToDoButton_Click" >Rediger</Button>
                    <Button x:Name="DeleteToDoButton" Width="50" Height="30" Background="Crimson" Click="DeleteToDoButton_Click" >Slet</Button>
                </StackPanel>


            </Grid>
        </DataTemplate>
    </Window.Resources>
    <StackPanel>
        <StackPanel Height="50" VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
            <Button x:Name="AddNewTodoButton" Height="25"  Width="100" Background="LimeGreen" Margin="10" Click="AddNewTodoButton_Click">Tilføj</Button>
        </StackPanel>
        <!--<Grid Margin="10" Background="LightSteelBlue">
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <CheckBox Margin="0, 0, 0 ,0" x:Name="CheckBoxDone" IsChecked="{Binding IsDone}" Checked="CheckBoxDone_Checked" Unchecked="CheckBoxDone_Unchecked" />
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="1"  VerticalAlignment="Center" HorizontalAlignment="Left">
                <TextBlock Margin="10, 0, 0 ,0" x:Name="TextBlockTitle" Text="{Binding Title}" />
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="2"  VerticalAlignment="Center" HorizontalAlignment="Left">
                <TextBlock Margin="10, 0, 0 ,0" x:Name="TextBlockDateOnly" Text="{Binding Title}" />
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="3"  VerticalAlignment="Center" HorizontalAlignment="Left">
                <TextBlock Margin="10, 0, 0 ,0" x:Name="TextBlockDone" Text="{Binding IsDone}" />
            </StackPanel>
        </Grid>-->
        <ItemsControl x:Name="TodoList" ItemTemplate="{StaticResource TodoItemTemplate}" />
    </StackPanel>
</Window>
